import { SET_CLOSEST_ENEMY } from "@game/store/action-types";

export const REDUX_NO_LOG_ACTIONS = [SET_CLOSEST_ENEMY];
export const ENEMY_TO_WINDOW_HEIGHT_RATIO = 0.15;
export const SPAWNABLE_AREA_LEFT_ADJUSTMENT = 4; // percentage
export const NUM_LEADERBOARD_ENTRIES = 10;
export const ABOUT_URL = "https://github.com/patricepeartree/typper-web";

export const Keys = Object.freeze({
    ENTER: "Enter",
    ARROW_UP: "ArrowUp",
    ARROW_DOWN: "ArrowDown",
    ESC: "Escape",
    SPACE: " "
});
